/**
 * @typedef {Object} SpecimenImage A record of the image ID against the specimen ID
 * @prop {?string} [imageID]
 * @prop {?string} [specimenID] //This is the identifier for the specimen after everything is removed...
 * @prop {?string} [imageBarcode] //basically the file name with the extension removed
 * @prop {?string} [projectID]
 */