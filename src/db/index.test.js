import { describe } from 'vitest'
import db from './index'

describe('db', _ => {
  it('should have function getRecord', _ => {
    
  })
})


, 
  addRecord, 
  setRecord, 
  updateRecord,
  deleteRecord,
  arrayAppend,
  arrayRemove,
  queryCollection